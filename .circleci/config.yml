version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.11.0
    steps:
      - Install dependencies
      - run: npm install
  test:
    docker:
      - image: circleci/node:10.11.0-browsers
    steps:
      - Run tests
      - run: npm test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test